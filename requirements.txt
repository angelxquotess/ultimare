# ================================================================
# JARVIS - Merged requirements (Mark-L base + daidai extensions)
# ================================================================

# --- Core (Mark-L) ---
PyQt6
PyQt6-WebEngine
sounddevice
soundfile
google-genai
google-generativeai
pillow
Pillow
requests
beautifulsoup4
duckduckgo-search
playwright
pyautogui
pyperclip
pygetwindow
opencv-python
numpy
mss
psutil
send2trash
youtube-transcript-api
python-pptx
fastapi
uvicorn[standard]
cryptography
python-multipart
qrcode[pil]

# --- Voice / audio extras (daidai) ---
pyaudio
playsound==1.2.2

# --- OCR / QR ---
pytesseract

# --- Map widget (daidai) ---
tkintermapview

# --- Messaging / Social (daidai) ---
telethon
instagrapi

# --- Env / utility ---
python-dotenv

# --- Windows-only (auto-skipped on other OS) ---
comtypes; sys_platform == "win32"
pycaw; sys_platform == "win32"
win10toast; sys_platform == "win32"
pywinauto; sys_platform == "win32"
pywin32; sys_platform == "win32"
